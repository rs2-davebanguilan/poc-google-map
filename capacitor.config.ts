import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'poc-google-map',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
